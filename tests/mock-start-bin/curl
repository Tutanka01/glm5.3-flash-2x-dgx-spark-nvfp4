#!/usr/bin/env bash
case " $* " in
  *" /v1/tokenize "*|*"/v1/tokenize"*) printf '{"count":2}\n' ;;
  *) printf '{"object":"list","data":[{"id":"glm-5.3-flash-nvfp4","max_model_len":32768}]}\n' ;;
esac
