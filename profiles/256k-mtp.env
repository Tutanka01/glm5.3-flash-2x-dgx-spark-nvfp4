# Quarantined short-decode experiment: 262 144-token server limit, CUDA graphs
# (bs=1) and MTP (NEXTN). Do NOT send a cold prompt above 128k with this
# profile on 128 GiB unified-memory nodes. The 240k attempt on 2026-08-27
# stalled during prefill and its scheduler was SIGKILLed. Use profile 256k for
# capacity, then use MTP only on the already validated 128k tier.
# Measured on the reference 2x GB10 fabric (2026-08-27, docs/BENCHMARKS.md):
# graphs at a configured 256k decode at 14.4 tok/s on short prompts, identical
# to 128k. This validates graph capture, not a cold 256k prefill. MTP is the
# remaining decode lever: it doubles mono-stream decode at 32k and 128k.
# bench-long-context.py now refuses this unsafe combination above 128k unless
# --allow-unsafe-profile is explicitly supplied.
# Bench with: ./bench-glm53.py --runs 3
MAX_MODEL_LEN=262144
MAX_NUM_SEQS=1
MAX_NUM_BATCHED_TOKENS=4096
MEM_FRACTION_STATIC=0.90
KV_CACHE_DTYPE=fp8_e4m3
MOE_BACKEND=flashinfer_cutlass
DSA_PREFILL_BACKEND=flashinfer_sparse_mla
DSA_DECODE_BACKEND=flashinfer_sparse_mla
CUDA_GRAPH_MAX_BS=1
DISABLE_CUDA_GRAPH=0
MTP_NUM_TOKENS=5
